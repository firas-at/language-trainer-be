# Step 1: Use the official Node.js image as the base image
FROM node:22-alpine

# Step 2: Set the working directory inside the container
WORKDIR /app

# Step 3: Copy package.json and package-lock.json (or yarn.lock) files into the container
COPY package*.json ./

# Step 4: Install the dependencies (including development dependencies)
RUN npm install

# Step 5: Copy the rest of the application code into the container
COPY . .

# Step 6: Expose the port (default for NestJS is 3000)
EXPOSE 3000

# Step 7: Install global dependencies for development
RUN npm install -g ts-node typescript

# Step 8: Command to start the NestJS app in development mode
CMD ["npm", "run", "start:dev"]
